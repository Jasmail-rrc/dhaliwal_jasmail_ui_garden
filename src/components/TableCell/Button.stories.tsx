import Button from "./Button";

const meta = {
  title: "Components/TableCell",
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "TableCell",
    disabled: false,
  },
};

export const Disabled = {
  args: {
    label: "Disabled TableCell",
    disabled: true,
  },
};
