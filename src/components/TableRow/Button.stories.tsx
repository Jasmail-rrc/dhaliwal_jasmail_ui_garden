import Button from "./Button";

const meta = {
  title: "Components/TableRow",
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "TableRow",
    disabled: false,
  },
};

export const Disabled = {
  args: {
    label: "Disabled TableRow",
    disabled: true,
  },
};
