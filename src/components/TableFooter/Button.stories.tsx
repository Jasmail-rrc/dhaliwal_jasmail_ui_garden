import Button from "./Button";

const meta = {
  title: "Components/TableFooter",
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "TableFooter",
    disabled: false,
  },
};

export const Disabled = {
  args: {
    label: "Disabled TableFooter",
    disabled: true,
  },
};
