import Button from "./Button";

const meta = {
  title: "Components/TableHeader",
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "TableHeader",
    disabled: false,
  },
};

export const Disabled = {
  args: {
    label: "Disabled TableHeader",
    disabled: true,
  },
};
