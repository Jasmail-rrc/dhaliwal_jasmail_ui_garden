import Button from "./Button";

const meta = {
  title: "Components/Img",
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "Img",
    disabled: false,
  },
};

export const Disabled = {
  args: {
    label: "Disabled Img",
    disabled: true,
  },
};
